Gives release number:
lsb_release -r

More universal command:
cat /etc/lsb-release

DISTRIB_RELEASE=
DISTRIB_CODENAME=

if [ RELEASEVERS = "14.04" ]
then

fi

string_contains() { [ -z "${2##*$1*}" ] && [ -n "$2" -o -z "$1" ]; }

Arguments:
$1

Broadcom ID command:
lspci -nn -d 14e4:

Discord download link
Note, installs to CWD.
sudo apt-get install wget
wget -nc ./AutoInstall https://discordapp.com/api/download?platform=linux&format=deb
